"""RailMind digital-twin engine.

Modular, typed railway corridor simulation. Every stage (data, detection,
prediction, optimization, verification) sits behind an interface so future
implementations drop in without rewrites. See ``railmind.interfaces``.
"""

__version__ = "1.0.0"
